<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="ExamenApp1.Views.RectanguloPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <StackLayout
        Padding="20"
        HorizontalOptions="CenterAndExpand"
        VerticalOptions="CenterAndExpand">
        <Label Text="Ingresa el radio:" />
        <Entry Keyboard="Numeric" Text="{Binding Radio}" />
        <Label Text="Resultado:" />
        <Entry IsReadOnly="True" Text="{Binding AreaRectagunlo}" />
        <Button Command="{Binding CalcularAreaRectanguloCommand}" Text="Calcular" />
        <Button Command="{Binding LimpiarCommand}" Text="Limpiar" />
    </StackLayout>

</ContentPage>