<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="ExamenApp.Views.CirculoPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <StackLayout
        Padding="20"
        HorizontalOptions="CenterAndExpand"
        VerticalOptions="CenterAndExpand">
        <Label Text="Ingresa el radio:" />
        <Entry Keyboard="Numeric" Text="{Binding Radio}" />
        <Label Text="Resultado:" />
        <Entry IsReadOnly="True" Text="{Binding AreaCirculo}" />
        <Button Command="{Binding CalcularAreaCirculoCommand}" Text="Calcular" />
        <Button Command="{Binding LimpiarCommand}" Text="Limpiar" />
    </StackLayout>

</ContentPage>